// Modules
import React from 'react';
import { Text } from 'react-native';

export default function Title ({ children }:any) {
  return <Text style={{ fontSize:16, marginBottom:20 }}>{children}</Text>
}
